import React from 'react';
import NavbarContainer from './navbar_container';
import Modal from './modal';

export default () => (
    <div>
        <Modal />
        <NavbarContainer/>
    </div>
);

